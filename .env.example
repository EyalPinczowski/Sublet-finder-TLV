# Both optional — omit this file entirely (or leave these unset) to skip
# Telegram notifications and just check `python scripts/matches.py` manually.
TELEGRAM_BOT_TOKEN=your-bot-token-from-BotFather
TELEGRAM_CHAT_ID=your-chat-id
